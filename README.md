# Text_to_speeh
Text to speech based application and make in flask framework .when u run the app.py file . Its gives a localhost URL , and redirect into the web page where user upload the text file and then model can train on this textual data and gives a appropraite response .firstly model speaks a greeting which is hard coded and then conversation start.


firts of all download the code and Extract the code, make a directory in the project name as Templates and paste the html file here and then make a new diredct which names is uploads to store the input throgh users and then run the app.py file 
